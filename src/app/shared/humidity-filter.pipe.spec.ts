import { HumidityFilterPipe } from './humidity-filter.pipe';

describe('HumidityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HumidityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
